---
description: Textbus 官方文档（docs/）编写规范——受众、API 深度、较长示例用 Tab（code-group）、可运行示例与在线编辑约定、统一用语、提纲与正文分离、禁止源码路径与元话语。
globs: docs/**/*
alwaysApply: false
---

# Textbus 文档编写规范

**品牌名称（硬性）**：对外叙述、标题、正文与 UI 文案中，产品名一律写作 **Textbus**。禁止使用 TextBUS、TEXTBUS、textBus、「Text Bus」等其它形式。（图示文件名、第三方存量外链除外；无障碍文案与正文用语保持一致时写作 Textbus。）

约定 **Textbus 官方文档**的受众、结构、写作流程与行文要求：`docs/` 中的 VitePress 站点**即为即将发布到 textbus.io 的唯一官方文档**，与线上站点是同一套内容，**不存在**「另一份官网文档」「以外部站点为准、本站仅供参考」等二元权威。增补或重写时遵守本规则；若有冲突，以本规则为准。

## 1. 受众与立场

- **阅读者**：使用 Textbus 构建富文本应用或编辑器的开发者。
- **非阅读者**：不以「在本仓库中开发 Textbus 源码、发 npm 包、跑 monorepo」为叙述对象；不在正文里写克隆仓库、playground、pnpm workspace 等与框架使用无关的流程。
- **作者立场**：文档由 Textbus 维护者对使用者编写，是**对框架能力与用法的权威说明**，语气直接、可执行，避免「我们假设你已经……」式省略。

## 2. API 与能力说明的深度

- 文中出现的 **API**（函数、类型、配置项、`@textbus/*` 包导出等）须具备：**用途**、**最小可运行或最小可理解示例**、**必要参数 / 返回值 / 与周边 API 的关系**、**注意事项与常见踩坑**（边界、顺序、`reflect-metadata`、各适配器包分工等交叉点）。
- 避免只列名字或一句话带过；篇幅有限时优先「能跑通的最小场景 + 一两句为何如此」。

### 2.1 代码示例较长时使用 Tab 分组

- 当示例 **偏长**（经验上约 **25 行及以上**），或同一知识点需要 **多段代码并排对照**（多文件、多语言、「精简版 / 完整版」等）导致正文被代码块连续占满时，**应使用 VitePress 代码 Tab**（`::: code-group`），而不是在同一屏垂直堆砌多个大块代码。
- **每个 Tab** 须在围栏语言标记后用 **`[标签]`** 标明可读名称（建议用语义或文件名，如 `[最小入口]`、`[editor.tsx]`、`[注册模块]`），便于切换与扫描。
- **短示例**（通常不足约 25 行且无需多 tab 对照）仍用普通 \`\`\` 代码块即可，不必为形式强行 Tab。
- **写法**（示意；详见 [VitePress：Code Groups](https://vitepress.dev/guide/markdown#code-group)）：

````markdown
::: code-group

```ts [a.ts]
// 较长或并列展示的代码 A
```

```ts [b.ts]
// 较长或并列展示的代码 B
```

:::
````

### 2.2 可运行示例与在线编辑（工程约定）

以下为用户已确认的 **文档站内嵌演示** 方向；**实现落地后**，涉及「既要可看源码又要可跑」的示例应优先遵守，避免又回到仅静态代码块或外链沙箱。

- **形态**：同一组件内用 **Tab** 切换 **「源码」** 与 **「预览」**，**不新开窗口**；不要求读者本地安装 Node 才能「看到动起来」的效果。
- **范围**：源码放在 **`docs/` 文档工程内**（含 `.vitepress` 侧运行时），通过 **monorepo workspace** 引用仓库内 **`packages/*`**（`@textbus/*` 等），**不把 StackBlitz / 外链演示站当作正文内嵌的运行环境**（站点导航保留「在线演示」等外链可作为补充，与内嵌无关）。
- **编辑与编译**：支持 **整文件（整段演示入口）在线编辑**；编译器 **异步按需加载**（例如首次进入预览 Tab 再拉 WASM/脚本），编译放在 **Web Worker**、编辑 **防抖** 后再触发；须具备 **加载中 / 编译中** 等异步反馈。**首包体积较大可接受**，但不得阻塞文档首屏（懒加载）。
- **与静态 Tab 的关系**：**§2.1** 的 **`::: code-group`** 仍用于 **只读**、多文件对照；**需要运行与编辑** 的示例在能力就绪后改用统一的 **内嵌 Playground / Demo 组件**（具体标签名以实现为准），不在正文重复维护两套脱节源码。
- **多套初始源码**：**`<TextbusPlayground preset="…" />`** 通过 **`docs/playground/presets/`** 切换文件树与 **`tabs`**；新预设实现 **`PlaygroundPreset`** 并在 **`presets/index.ts`** 的 **`PLAYGROUND_PRESETS`** 中注册（键名即 **`preset`** 取值）。**`getting-started`** 与 **`component-basics`** 分离：后者在副本上扩展 Todolist，勿改前者以免影响快速开始页。
- **诚实边界**：浏览器端编译链与本地 **`tsc` + Vite** 对 **装饰器、`emitDecoratorMetadata`** 等未必完全一致；若某示例在线与本地行为不一致，须在当页 **注意** 中说明或收敛示例写法。

## 3. 全站结构：先提纲、后建文件

- 新建或大规模调整目录前：先写 **全站/全册提纲**（目录骨架 + 章节归属 + 各块职责与顺序），以 Markdown 放在 **仓库根目录**（如 `DOCS-OUTLINE.md`），**经作者确认或修改后**再在 `docs/` 下创建、移动对应文件。
- **目录与章节骨架一律用树状结构写出**，便于扫一眼看清层级、父子关系与文件归属；不要用「一段散文 + 扁平列表」代替整站结构图。
- **推荐写法**：一层展示 `docs/` 下路径（目录用 `/` 结尾），叶子为 `.md` 文件名或路由含义；每个节点后用 **冒号或括号** 跟 **一句话职责**（可选侧栏分组名用 `[侧栏: xxx]` 标在分支上）。
- **字符**：优先使用 **ASCII 树形线**（`├──` `└──` `│`）写在 Markdown 代码块或等宽排版中；若环境不便显示，可用 **缩进 + `-`** 模拟同等深度的缩进树（每层固定 2～4 空格，严禁深度错乱）。
- 未确认提纲前不批量新建空页面。
- **由浅入深**：**准备**（简介、快速开始）之后接 **入门**（**`component-basics.md`** → **`text-styles.md`** → **`block-styles.md`** → **`selection.md`** → **`operations-and-query.md`**（侧栏 / 标题：**状态查询与基础操作**，正文先 **`Query`** 再 **`Commander`**）→ **`history.md`** → **`shortcuts-and-grammar.md`** → **`component-events-and-lifecycle.md`** → **`document-parse-compat.md`**），每页写清动机、概念与步骤；**`concepts.md`** 作名词对齐；**`slot.md`** 专讲 **`Slot`**（**`schema`**、游标、**`insert` / `cut` / `delta`** 等）；**`component-advanced.md`**（**`separate`**、**`removeSlot`**、**`deleteAsWhole`**、**`getSlots()`** 顺序与命令协作）列在 **概念与进阶**，供多插槽块作者查阅；**`guide/adapter-*.md`** 视图适配器与 **`guide/collaborate.md`** 协作在 **VitePress 侧栏** 中紧接其后；**`editor-and-modules.md`** 等承载 Registry、插件、`providers` 等放进阶内容。**`docs/` 正文不向读者描述站点目录策划、侧栏分组或撰稿意图**（维护说明写在仓库根目录提纲 / 本规则即可）。

**全站提纲树示例（形式示意）：**

```text
docs/
├── index.md                    # 首页
├── guide/
│   ├── getting-started.md      # 安装与最小可运行编辑器
│   ├── packages.md             # 各 @textbus/* 包参考入口
│   ├── package-core.md         # 示例：内核包导出索引
│   └── ...
```

## 4. 单篇文档：先章节规划、后正文

- 写某一具体 `.md` 正文前：先写该文件的 **章节规划**（规划中的 **二/三级标题** + 每节一句话意图），放在 **仓库根目录** 独立文件（如 `DOCS-page-router.md`），**经作者确认或修改后**再写入 `docs/` 对应正文。
- **章节规划一律用树状结构写出**：根节点写 **目标稿件路径或页面标题**，子节点对应规划中的 **`##` / `###` 标题文字**（不写 `#`），叶子节点附 **本节意图**（一句话）；不要把章节顺序只写在散文里而不给出可扫读的层级树。
- **同一页的备选阅读顺序**若需标明，在树附近单起一小段，或用 `(1)(2)` 标在树节点行尾；仍以树为主体。
- 单篇内遵守「从简单到深入」（见第 7.2 条）。
- **规划稿留在根目录**；勿把与规划文件一一对应的「全文章节树」「阅读顺序树」等**撰稿提纲**粘贴进 **`docs/`** 正文（见第 6.4 条）。侧栏「本页目录」由 VitePress 根据标题自动生成。

**单篇章节规划树示例（形式示意）：**

```text
docs/guide/getting-started.md
├── 你会学到什么                  # 文首：目标与前置（不写关键词表）
├── 安装依赖
│   ├── 最小依赖组合              # core + platform-browser + 某一 adapter
│   └── TypeScript / 构建注意     # reflect-metadata 等与入门相关的配置
├── 挂载第一个编辑器              # 端到端可运行示例
└── 常见问题                      # 错误写法与排错置底
```

## 5. 基础与高级两档

### 基础

- 以 **使用案例、端到端小流程** 为主，帮助尽快建立 **整体心智模型**（如安装 → 接入平台层 → 挂载编辑器 → 改文档数据 → 看到更新）；可照抄示例优先于抽象罗列。

### 高级

- 写全 **API 细节**：签名语义、可选与默认、组合方式、与相关 API 的分工；**可适当**说明设计意图或协作与数据流层面的原理。
- **禁止**：在文档中直接引用本仓库源码路径或行号，或写「详见 `packages/xxx` 某文件实现」。原理用叙述 + 概念；实现细节以类型定义与行为为准。

## 6. 官方性与时效表述

- **当前编写的 `docs/` 内容即 textbus.io 官方文档**：不向读者暗示「正式文档在 textbus.io、本站只是镜像/草稿」「请以旧站为准」等；**textbus.io 与本仓库文档同源发布时，权威只有这一处叙述**，不因域名不同而分裂。
- **外链**：可指向 **演示 / Playground**、**GitHub**、**社区或第三方包**，但不要写成「完整文档请去 textbus.io」（除非文案明确表示「当前页的线上副本」之类技术性跳转，且读者已知二者同一内容）；避免「旧版官网」「仅供参考」「可能过时」「请参阅某外部站点获取权威说明」等把文档权威让渡给**另一套文档**的表述。
- 与已发布包行为不一致时，以 **修正文档或发版说明** 解决，不在正文里让读者自行猜版本。

## 6.1 禁止「写作元话语」

- **不向读者**说明「本页/本站参考某外部文档、某框架官网的结构或目录编写」「与 xxx 指南同构」等——那是内部撰稿方法，与使用框架无关。
- 需要对比其他技术时，只写 **Textbus 的行为与差异**（客观、可验证），不交代「对照谁写的文档」。
- 若维护者需要在仓库根目录提纲（如 `DOCS-OUTLINE.md`）里记录与外部资料的对照，可保留给 **维护与协作**；**`docs/` 下读者可见正文**不出现上述元叙述。

## 6.2 只用 Textbus 语境，避免无关外部框架术语

- **不以其它前端框架的专属概念定义或衬托 Textbus**（例如「类似 xxx 的组件写法」「没有 xxx 的生命周期钩子」）。读者未必熟悉那些框架，这类表述容易把文档写成对照表。
- **应写清 Textbus 自身事实**：例如 **组件 / 插槽 / 格式（formatter）/ 属性** 数据模型、**选区与命令**、**`@textbus/core`** 与 **`@textbus/platform-browser`** 分工、**视图适配器**（**`@textbus/adapter-viewfly`** / **`adapter-vue`** / **`adapter-react`**）、协作 **`@textbus/collaborate`** 等——用本仓库包名与公开 API 名称说明即可。
- **允许出现的「外部」名词**：通用基础设施与开放标准（如 **Node.js**、**npm**、**pnpm**、**Vite**、**TypeScript**、**ES 模块**），以及宿主框架在适配器语境下的名称（**Viewfly**、**Vue**、**React**）仅作为「通过哪个适配器渲染」的客观标签；避免用「别家的 API 名」当 Textbus 章节的标题或定义轴心。

## 6.3 使用教程与实现细节分离

- **指南正文**（尤其 **准备**、**入门** 分组下的页面）按**使用教程**写：给出**可复制的写法**、**在页面上 / 在类型上能观察到的效果**、**常见误用与注意点**。
- **禁止**在指南正文里用**内部实现**来教用法：不出现具体私有函数名、内核内部流水线步骤，或未在**公开 API / 公开 `d.ts`** 中承诺的实现细节链条。说明「支持哪些写法」时，用**用户手里的类型与公开入口**，不要用源码里的调用顺序当教案。
- **对外导出的类型名**可作为 **TypeScript** 约束引用，但不必展开为「内部如何解析」；一句「见类型提示」或示例即可。
- **深入**等偏机制章节若需原理，用**概念级**叙述（数据模型、同步、协作与会话等），仍遵守第 5 条**禁止仓库路径与行号**。

## 6.4 正文不收撰稿中间产物

- **`docs/` 下面向读者的 `.md`** 只保留**教程与参考内容**：不出现与 **`DOCS-page-*.md`**、内部提纲**逐节对位**的「全文章节树」「阅读顺序树」等——那是写稿时的**目录草稿**，与学会使用框架无关；重复一遍也会与侧栏「本页目录」冗余。
- **禁止**单独成节的「撰稿说明」式内容：如 **「本页讲什么」「与谁衔接」** 下再列 **「本章：…」「不展开：…」**——与文首引言重复，读起来像作者手记而非用户文档。
- **文首引言**只交代：本页**要教会什么**、**必要前置**（例如须先完成的安装或上一节阅读）。**禁止**在文首用一整段罗列「本页还会出现的 API 名 + 各文档链接」——读者尚未进入语境，容易变成关键词表；与其它章的跳转放在**首次自然写到该能力**时再用一两句带出即可。
- **错误写法后置**：正文主流程优先展示**正确写法**与可运行示例；错误写法统一放在靠后的**注意点 / 排错**小节，且每条都要给出对应改法。避免在开篇或主干段落先铺大量错误示例，影响阅读连贯性。
- **可以保留**的图示：用**用户语言**帮助理解**概念或写法**的短树或列表（例如组件树、格式作用范围），须直接服务当前知识点，而非复制维护者专用规划结构。
- **验收**：发布前检查是否误贴了「本章节目录预览」类块；有则删或改写为概念说明。

## 7. 叙述顺序与密度

### 7.1 Textbus 相关用语（统一）

- 指 **富文本节点树中的 UI 单元**：用 **「组件」**；与 DOM 通用名词冲突时，用 **「Textbus 组件」** 或上文已限定在编辑器模型时可单称 **「组件」**。
- 指 **组件内承载子内容的位置**：用 **「插槽」**。
- 指 **可应用到内容的样式或标记规则**：用 **「格式」** 或 **`formatter`**（首次出现可写「格式（formatter）」）。
- 指 **节点上的元数据键值**：用 **「属性」** 或 **`attribute`**（同上可并列一次）。
- 指 **用户光标与选中的范围**：用 **「选区」**；涉及 API 时保留类型或方法名中的英文（如 API 文档标题可与符号一致）。
- 指 **把文档模型交给 Viewfly / Vue / React 绘制**：用 **「视图适配器」** 或直接写包名 **`@textbus/adapter-*`**。
- **`Commander` / `Query` / `History`** 等 **公开 API 标识**：与类型或文档一致，勿自造中文替换名当唯一称谓。
- **Viewfly 视图代码示例**：**插槽以外**的静态 DOM（勾选框、布局外壳等）用 **JSX**；**`adapter.slotRender` 第二个参数（渲染工厂）**内用 **`createVNode`** 包住内核传入的子节点，与格式树 / 虚拟节点衔接一致。

### 7.2 叙述顺序与密度（续）

- **整体顺序**：从简单到深入；同一主题先「能做什么」再「边界与细节」。
- **禁止**：一句话带过关键步骤；默认读者已会某**工具**（如命令行、包管理器）而跳过必要说明时，仍须写清与本框架相关的配置；**禁止**默认读者已会「另一套富文本方案」而省略 Textbus 概念。
- **应写清**：前提、配置项含义、示例与句子的对应关系；必要时用短列表拆步骤。
- **交叉引用**：链到其它页面时，放在读者**已经遇到该问题或该 API** 的上下文中（示例后、注意点、小结一句）；避免在文首或独立小节里「预告式」堆叠链接与名词。

## 附：与工程脚本

- 根目录 `package.json` 的 `docs:dev` / `docs:build` 仅用于本地预览与构建；**不在**文档正文中写「如何参与本仓库开发」类流程。
- **对照其它本地仓库**（例如旧版官网文档工程、XNote 克隆）：需要时在 **当次对话中由维护者提供路径**；规则与 Git **不记录**本机路径。撰稿辅助仍以 **`DOCS-OUTLINE.md` §5**、公开仓库与本仓库 `packages/*` 为准。

修订本规则时同步更新本文件。
